---
description: 文档文件命名与目录规范（docs/，按月/日二级目录）
globs:
  - "docs/**"
  - "**/*.md"
alwaysApply: true
---

# 文档目录规范

## 唯一存放位置：`docs/`

所有 AI 生成的需求文档、方案文档、分析文档，**必须**放在项目根目录下 `docs/` 中，按 **月 → 日** 两级文件夹存放：

- **正确路径**：`docs/yyyy年M月份/D日/文档标题.md`
- **示例**：`docs/2026年8月份/23日/CTI项目说明.md`
- **月份文件夹**：`yyyy年M月份`（月份**不补零**，如 `1月份`、`8月份`、`12月份`）
- **日期文件夹**：`D日`（日**不补零**，如 `1日`、`9日`、`23日`）
- **日期取值**：文档**创建当天**，不是工作完成日
- **同一天**的多个文档放在同一「日」目录
- **文件名**：中文或有意义的短标题，无需在文件名里重复日期

版式仍用 `md-doc-iteration.md`（文首版本、迭代记录、【vX.Y】大纲），不用 `yyyy/MM/dd` + 双栏。

## 禁止路径

- **禁止**写入工作区上级 `d:\vsWorks\cti\docs\`（不要用仓库根以外的 `docs/`）
- **禁止**写入 `.cursor/docs/` 或 `.trae/docs/`
- **禁止**在 `docs/` 根目录直接新建分析/方案类 md
- **禁止**只建「月」不建「日」
- **禁止**把新建方案写成 `docs/yyyy/MM/dd/名称-vX.Y.md`（那是历史版式）

## 历史文档

`docs/` 根目录以及 `docs/2026/08/21/` 这类旧日期目录作为历史基线保留，不批量搬家。  
**新建**分析/方案/排查类 md 一律进 `docs/yyyy年M月份/D日/`。

## AI 自检

每次生成 md 文档前确认：
1. 路径是否为 `docs/{今天yyyy年M月份}/{今天D日}/xxx.md`？
2. 是否误写到 `docs/yyyy/MM/dd/`？若是，改到月/日中文目录。
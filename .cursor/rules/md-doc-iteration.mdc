---
description: 需求/方案 md 文档的版本迭代与开发进度追踪规范
alwaysApply: true
---

# 需求文档迭代与进度追踪规范（强制）

## 适用范围

所有 AI 生成的需求分析、方案设计、问题排查类 md 文档（路径规范见 `doc-naming.md`：`docs/yyyy年M月份/D日/标题.md`）。

## 工作流：先文档、后开发、随时回写

1. **分析需求 → 先写方案 md**：列出现状分析、方案设计、改动文件清单、**待确认决策点**；
2. **用户确认 → 更新文档再动手**：把「待确认」改为「已确认：具体选择」，被否掉的方案**不删除**，用 `~~删除线~~` 或「已废弃」标注并注明原因；
3. **开发完成 → 回写实施记录**：实际改动文件清单、与原方案的偏差、编译/验证结果、遗留待办；
4. **后续每次迭代**（新增需求、改方案、修 bug）都在同一份 md 上追加，**不另开新文档**（跨天的大变更除外，按 doc-naming 建新的「月/日」目录并在旧文档头部加跳转链接）。

## 文档头部元信息（每份 md 必须有）

紧跟标题的引用块，包含：

```markdown
# 文档标题

> 创建：2026-07-09 ｜ 当前版本：v1.2 ｜ 状态：✅已完成
> 页面/模块：xxx（涉及的主要文件）
> 状态说明：一句话描述当前进展（如「方案 D 已上线，待验证多登互踢」）
```

状态取值：`⏳方案待确认` / `🔨开发中` / `✅已完成` / `🚀已上线` / `❌已废弃`。**每次状态变化必须同步更新此行**。

## 版本迭代记录（每次修改必须追加）

文档**末尾**固定「迭代记录」章节，每次实质性修改 md（加需求、改方案、确认决策、标进度）追加一行，**新行在上、旧行在下**：

```markdown
## 迭代记录

| 版本 | 时间 | 变更内容 |
|---|---|---|
| v1.2 | 2026-07-09 21:30 | 开发完成：回写实施记录（10.5节），编译通过 |
| v1.1 | 2026-07-09 15:20 | 用户确认方案D；环境隔离改为独立Redis实例，-tests方案标废弃 |
| v1.0 | 2026-07-09 10:00 | 初版：三个问题根因分析 + 方案A/B/D对比 |
```

版本号规则：

- 首版 `v1.0`；
- 方案调整、新增章节、决策确认、进度回写 → **minor +1**（v1.1 → v1.2）；
- 需求发生根本性变化（推倒重来）→ **major +1**（v2.0）；
- 错别字、排版微调**不记版本**；
- 时间精确到分钟（`yyyy-MM-dd HH:mm`），变更内容一行说清「加了什么/改了什么/确认了什么」。

## 进度标记规范

- **实施清单用表格 + 状态列**：每项标 `✅` / `🔨` / `⏳` / `❌`，完成项注明关键实现点；
- **章节标题带完成标记**：当某个章节/模块的开发任务全部完成后，在该章节标题末尾追加 ` ✅`，使左侧目录（大纲）中能直观看到哪些模块已完成。例如：
  - `### 10.8 防切屏/锁屏 + iOS 特殊处理 ✅`
  - `### 10.9 实施清单（完整版）✅`
  - 未完成的章节不加标记，保持原样
- **决策点集中管理**：单独章节列出所有待确认项，确认后原地更新为「已确认：xxx（确认时间）」，不新开章节；
- **开发偏差必须记录**：实际实现与方案不一致的地方（如临时改了口径、多改了一个文件），在实施记录中显式标注，不允许文档与代码悄悄脱节。

## 本版改动大纲标识（强制，方案 A）

目的：长文档迭代后，用户只需扫**左侧大纲**即可定位「这一版改了哪几节」，无需通读全文。

> 说明：左侧大纲**不支持**背景色 / 下划线 / 加粗等样式（编辑器只抽标题纯文本）。因此用标题最前面的 `【vX.Y】` 文本作唯一可靠高亮。

### 规则

1. **每次升版并写入实质改动时**，在本版**实际改动或新增**的章节标题中，把 `【vX.Y】` 放在**标题文字最前面**（`#` 后、正文标题前），与头部「当前版本」一致。  
   - ✅ 正确：`### 【v1.12】12.7 与 PC adm 批改逻辑对照`  
   - ❌ 错误：`### 12.7 与 PC adm 批改逻辑对照 【v1.12】`（版号在末尾，窄大纲易被裁切）
2. **只保留最后一个版本的标记**：升到新版本前，先全文去掉上一版的 `【vA.B】`（搜索 `【v`），再给本版改动节打上新标记。大纲里永远只出现**当前版本号**。
3. **历史对照**仍靠文末「迭代记录」表格，不在旧标题上堆叠多版号。
4. **标记范围**：仅标题（`##` / `###` / `####`）；正文内不必重复打版号。若本版只改了某节内几段，仍给该节标题打标记即可。
5. **与完成标记并存**：本版号在最前，完成 `✅` 仍放标题末尾。示例：`### 【v1.3】10.8 防切屏 ✅`；仅本版改动：`### 【v1.12】12.7 与 PC 对照`。
6. **不打标**：错别字/纯排版、仅改迭代记录表、仅改头部「当前版本」数字本身——这些不单独给章节打 `【v】`（若本版没有任何正文节变更，可不打标题标记，迭代记录仍要写）。
7. **寸止/回复**：更新 md 后，向用户说明「请看大纲中标题以 【vX.Y】 开头的节」；不必再贴大段全文。
8. **不要用** HTML/`<u>`/`**加粗**` 试图给大纲上样式——对左侧大纲无效，且可能把裸标签露进大纲。

### 示例（升版时）

```markdown
<!-- v1.11 → v1.12：先清除所有 【v1.11】，再标记本版改动节 -->
### 【v1.12】12.7 与 PC adm 批改逻辑对照
### 8.1 员工端隐私：禁分享 + 禁截屏（已确认）   <!-- 本版未改则不要挂版号 -->
```

## 节内交叉引用跳转（强制）

正文出现「见 §X.Y」「详见 §N」「参见 §…」时，**必须**做成可点击的 Markdown 链接，点击后跳到对应章节；**禁止**只写纯文本 `§3.3`（预览/阅读时无法跳转）。

### 写法

1. **目标节标题的上一行**放稳定锚点（**不要**写进标题行，避免 `<a>` 进左侧大纲）：

```markdown
<a id="sec-3-3"></a>
### 3.3 零二级索引表
```

2. **引用处**用链接：

```markdown
见 [§3.3](#sec-3-3)（`callRecord` / `appUploadLog` 等）
```

### 锚点命名

- 格式：`sec-` + 节号，`.` 改为 `-`
- 示例：`§3.3` → `sec-3-3`；`§4.6` → `sec-4-6`；`§4.5.6` → `sec-4-5-6`；`§10` → `sec-10`
- 同一文档内锚点全局唯一；升版改标题文案时**不要改**已有 `id`（避免旧链接失效）

### 适用范围

- 同文档内跳转：一律用上述锚点链接
- 跨文档跳转：用相对路径链接到目标 md（可带 `#sec-…`），例如 `[学情中台总览](./学情数据中台/00-总览与决策记录.md)`
- 迭代记录表格、纯历史叙述里的「§」若不需要跳转，可不改；**正文待办/结论/清单里的「见 §」必须可点**

## 方案编号 / 决策代号跳转（强制）

目的：复盘时在「已确认 / 待做」汇总表里点 `B3`、`S1`、`L3` 等即可跳到定义与详述，不必全文搜索。

### 何时必须加链接

以下位置出现**文档内已定义**的方案/层级/策略编号时，须做成可点击链接（同一单元格可多个链接）：

- 「已确认决策」「待确认」「待做 / 实施清单」「结论」类表格
- 文首/节首的路径一句话（如「走 B3+B4」）

常见编号形态（不限于）：`B1`/`B2`/`B3`…、`S1`/`S2`…、`L1`/`L2`/`L3`…、`方案A`/`方案D`、`模式A` 等。

### 锚点写法

1. 在编号**首次完整定义**处上方放稳定锚点（勿写进标题行）：

```markdown
<a id="scheme-b3"></a>
<a id="scheme-b4"></a>
### 5d.1 方案编号对照
```

2. 汇总表引用：

```markdown
| D1 | 产品路径 | [B3](#scheme-b3) + [B4](#scheme-b4)；[B1](#scheme-b1) 不做 |
```

### 锚点命名

- 方案/策略/层级编号：`scheme-` + 小写代号（`B3`→`scheme-b3`，`S1`→`scheme-s1`，`L3`→`scheme-l3`）
- 详述小节仍用 `sec-x-y`；编号锚点可与章节锚点并存（编号锚点落在定义表/定义段前）
- **不要改已有 id**（避免旧链接失效）

### 自检

- [ ] 汇总表里的 B3/B4 等是否已是 `[B3](#scheme-b3)` 形式？
- [ ] 目标处是否有对应 `<a id="scheme-…">`？
- [ ] 「见 §x.y」是否仍按上一节做成节锚点链接？

## 数据库变更规范（强制）

涉及数据库变更（新增表、添加/修改字段、加索引等）的需求，必须遵循以下规则：

### SQL 脚本归属

- **有配套 md 文档的需求**：SQL 脚本（DDL + DML）**必须内嵌到 md 文档中**（作为代码块），不单独生成 `.sql` 文件
- **没有 md 文档的需求**（用户未要求写 md）：SQL 脚本可单独生成 `.sql` 文件

### 新增表必须完整设计

新增数据库表时，md 文档中必须包含以下内容：

1. **建表 SQL**：完整的 `CREATE TABLE` 语句
2. **主键**：明确主键字段和自增策略
3. **外键关系**：列出所有外键及其引用的目标表/字段
4. **索引**：根据查询场景设计必要的索引（唯一索引、普通索引、组合索引）
5. **导航属性**：对应的 C# 实体类中需要添加的导航属性（`[ForeignKey]`、`[InverseProperty]`）、`virtual` 集合等
6. **字段说明表**：用表格列出每个字段的名称、类型、是否可空、默认值、用途说明

### 修改现有表

添加/修改字段时，md 文档中必须包含：

1. **ALTER TABLE SQL**：包含 `IF NOT EXISTS` 等幂等判断
2. **索引变更**：新字段是否需要建索引
3. **历史数据处理**：是否需要数据迁移/补录，如需则包含 UPDATE 脚本
4. **实体类变更**：对应的 C# 实体属性、导航属性变更

### 示例格式

```markdown
## 数据库变更

### 新增 testNotification 表

| 字段 | 类型 | 可空 | 默认值 | 说明 |
|---|---|---|---|---|
| testNotificationId | INT | NOT NULL | IDENTITY | 主键，自增 |
| periodId | INT | NOT NULL | - | 期次ID，FK → period |
| ...

**索引：** `IX_testNotification_periodId` (periodId)
**外键：** periodId → period.periodId

\`\`\`sql
CREATE TABLE [dbo].[testNotification] (...)
\`\`\`

**实体类变更：**
- `testNotification.cs`：新增实体类
- `period.cs`：添加 `ICollection<testNotification> testNotifications` 导航属性
```

## AI 自检（每次修改 md 前）

1. 头部元信息的「当前版本 / 状态」是否随本次修改同步更新？
2. 末尾「迭代记录」是否追加了本次变更行（新行在上）？
3. 被否掉/替换的方案是否保留并标注了废弃原因？
4. 开发完成后是否回写了实际改动清单与验证结果？
5. **涉及数据库变更时**：SQL 脚本是否已内嵌到 md 文档中（而非单独 .sql 文件）？新增表是否包含主键、外键、索引、导航属性的完整设计？
6. **本版大纲标识**：是否已清除上一版所有 `【v旧】`？本版改动/新增的章节标题是否已在**最前面**挂上 `【v当前】`（非末尾）？
7. **节内跳转**：正文「见 §X.Y」是否已写成 `[§X.Y](#sec-x-y)`，且目标节上方有对应 `<a id="sec-x-y"></a>`？
8. **方案编号跳转**：已确认/待做等汇总表中的 B3、S1、L3 等是否做成 `[B3](#scheme-b3)`，且定义处有 `scheme-*` 锚点？

## 历史文档

规则生效前的存量 md 不强制补迭代记录；**下次修改到某份旧文档时**，顺手补上头部元信息和迭代记录章节（首行版本按当前内容记 v1.0，历史变更可合并为一行「v1.0 之前的历史演进见正文」）。